# Solanum development

## Releasing

solanum binaries are built by a CircleCI workflow that runs on commits to
`main` and uploaded as CircleCI artifacts. To download them, navigate to one of
the "build" jobs, then the artifacts tab.
